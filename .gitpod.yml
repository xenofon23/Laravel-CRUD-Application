image:
  file: .gitpod.Dockerfile
tasks:
  - name: Start MySQL Server and Create Database
    command: |
      service mysql start
      echo "CREATE DATABASE crud;" | mysql
      echo "CREATE USER 'superadmin'@'localhost' IDENTIFIED BY 'password'; GRANT ALL PRIVILEGES ON *.* TO 'superadmin'@'localhost' WITH GRANT OPTION;" | mysql